{
  "name": "xyz.example.getting.started",
  "version": "1.0.0",
  "description": "examples in the getting started section of xyz",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec", 
    "test-travis": "rm -Rf node_modules && npm install && ./node_modules/.bin/mocha --reporter spec"
  },
  "author": "kian.peymani@gmail.com",
  "license": "ISC",
  "dependencies": {
    "xyz-cli": "^0.4.1",
    "xyz-core": "^0.4.1"
  },
  "devDependencies": {
    "mocha": "latest",
    "chai": "latest"
  }
}
